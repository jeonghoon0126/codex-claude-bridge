{
  "name": "claude-channel-codex-bridge",
  "version": "0.3.0",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "bridge": "bun covering-bridge.ts",
    "server": "bun bridge-server.ts",
    "claude-mcp": "bun claude-mcp.ts",
    "codex-peer": "bun codex-peer.ts",
    "codex-mcp": "bun codex-mcp.ts",
    "start": "bun install --no-summary && bun bridge-server.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.10"
  }
}
