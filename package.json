{
  "name": "claude-channel-codex-bridge",
  "version": "0.3.0",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "bridge": "bun covering-bridge.ts",
    "server": "bun bridge-server.ts",
    "web": "bun codex-mobile-web.ts",
    "web:tunnel": "bun web-local-tunnel.ts",
    "web:vpn": "bun web-vpn.ts",
    "web:cf:setup": "bun web-cloudflare-setup.ts",
    "web:cloudflare": "bun web-cloudflare.ts",
    "web:service:install": "bun web-launchd.ts install",
    "web:service:uninstall": "bun web-launchd.ts uninstall",
    "web:service:status": "bun web-launchd.ts status",
    "sms": "bun twilio-codex-sms.ts",
    "sms:tunnel": "bun sms-local-tunnel.ts",
    "sms:configure": "bun twilio-configure-webhook.ts",
    "codex-peer": "bun codex-peer-agent.ts",
    "claude-mcp": "bun claude-mcp.ts",
    "codex-mcp": "bun codex-mcp.ts",
    "start": "bun install --no-summary && bun bridge-server.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.10"
  }
}
